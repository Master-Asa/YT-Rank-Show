# v0.9.2

- 更新來源改為 Master-Asa/YT-Rank-Show，支援從已切換新來源的 0.9.0 檢查、下載並升級。
- 不變更排行榜、外觀、支持紀錄或資料格式；本次主要驗證正式 Release 更新流程。

# v0.9.0

- 新增正式 Release 更新檢查、可選的背景定期檢查、更新包下載與驗證，以及等待 OneComme 關閉後執行的獨立 Windows 更新視窗。
- 更新前備份程式與資料；替換失敗時嘗試回復舊版。只支援指定 GitHub 公開正式版本，私人／Pre-release 不自動安裝。
- 新增隔離舊版升級、下載毀損拒絕、版本限制及安裝故障還原測試，不使用正式支持紀錄。

# v0.8.7

- 合併本次尚未發布的名次修正：「圖示＋數字」保留缺圖示空位並對齊數字；「只有圖示」讓 emoji 與替代數字在共同欄位置中，名稱維持對齊。
- 整欄對齊新增數值欄位置調整，可將金額整欄拉近名稱並保持右側對齊。
- 簡化排列與間距：跟隨名稱只顯示名稱到數值間距，整欄對齊只顯示數值欄位置；保留各榜原有設定與名稱長度限制。

# v0.8.6

- 修正「只有圖示」的名次對齊：emoji 與缺少圖示時的替代數字置中於共同名次欄，名稱起點保持對齊，不影響「圖示＋數字」排列。

# v0.8.5

- 修正「圖示＋數字」名次排列：沒有 emoji 的名次保留圖示欄寬，數字名次與觀眾名稱各自對齊；圖示大小變動時同步調整。

# v0.8.4

- 圖示＋數字、只有圖示與數字名次使用共同寬度，前三名與第 4～10 名的名稱起點對齊；字型或 emoji 大小改變時同步重算。
- OBS 外觀預設隱藏名稱開頭的 @；可在每張榜「內容 → 進階名稱設定」關閉。只影響顯示，原始名稱、使用者識別與統計不變。
- Release 標題簡化為版本名稱，說明只列本次更新。

# v0.8.3

- 修正深色介面空排行榜的淺色背景與淺色文字衝突；改為清楚的空榜狀態卡，原因可展開，保留前往頻道設定操作。
- 本次私人測試版包含 v0.8.1 的深淺色與紀錄篩選，以及 v0.8.2 的功能分區與文字精簡。

# v0.8.2

- 管理介面精簡重複說明，頻道、追蹤、提醒、手動加入、場次管理、匯出與備份改為清楚的功能區塊，支援深淺色與窄視窗。
- 手動加入預設收合；較長解釋移至區塊內說明，保留聯外同意、OBS 擷取提醒、錯誤與破壞性操作確認。
- 不變更統計、收錄、場次歸屬、匯入或資料格式。

# v0.8.1

- 管理頁新增深色、淺色與跟隨系統配色，預設深色；僅記住瀏覽器偏好，不更動 OBS 外觀或統計設定。
- 支持紀錄新增狀態篩選，區分待核對與不計入；保留詳情與完整原因。
- 場次管理新增紀錄年份／月份、未加入狀態篩選及每頁 20 場分頁，精簡選取清單搭配單場資訊欄；加入、移出與查詢仍需明確操作。
- 測試包含 300 場跨年紀錄、主題保存／系統切換、OBS 預覽隔離及原有加入／移出／再次加入流程。

# v0.8.0

- 新增已加入／未加入的場次管理，支援備份後移出與重新加入。移出狀態阻擋背景自動加回；原始紀錄不刪除，重複操作不複製事件。
- 優先顯示直播標題，支援搜尋及經同意查詢公開資訊，結果保存在本機。已查明其他頻道的影片不允許加入。
- 移除已被取代的舊歸屬表格與永遠不成立的 URL 長度提示判斷；保留舊 OBS 入口與必要相容程式。
- 增補第三方元件、Bootstrap 授權說明與乾淨包檢查規則。
- 歸屬判斷只讀取單場必要欄位，不再逐事件複製整份標題目錄；CSV 匯出共用一次整理好的歸屬對照。
- 未使用的舊資料物件暫時被 Windows 占用時，有限次重試整理；現行與前一版仍引用的物件不刪除，持續失敗會保留警告與錯誤代碼。

# v0.7.3

- OBS 與歷史匯入操作列貼齊工作區畫面底緣，依按鈕列實際高度保留內容空間，支援窄視窗換行。
- TXT 設定與輸出路徑移至「OBS 外觀與輸出 → 純文字輸出（TXT）」，共用原設定欄位，不改統計及儲存格式。

# v0.7.2

- 歷史匯入改為精簡選取清單與單場資訊欄，集中批次查詢、全選與取消選取；底部保留匯入操作。詳細資料預設收合，查看場次不改變勾選，窄視窗上下排列。統計與核對規則不變。

# v0.7.1

- 歷史匯入卡片明確分開「選取這場直播」、影片標題與影片 ID。點標題不再切換選取；缺少標題改顯示「尚未取得影片標題」。統計與匯入規則不變。

# v0.7.0

- 歷史匯入預覽可經同意查詢公開影片標題、頻道名稱與 ID，分為相符、不符、無法確認。顯示資訊來源、原始標題、影片連結及紀錄時間範圍，不把最早留言時間当開台時間。
- 相符結果可供匯入；不符由後端阻擋，未查詢／無法確認須逐場人工確認。查詢只保留本次預覽，未按匯入不寫入紀錄。
- 批次查詢每次最多 10 場、逐支執行，可停止後續查詢。查詢不佔用即時收錄存檔佇列，頻道／預覽變更後丟棄過期結果。
- 保留 v0.6.9 的系統 DNS 相容性修正與查詢安全限制。

# v0.6.9

- 恢復與舊版 fetch 相同的系統名稱解析路徑，改用 Windows／作業系統 lookup，不再由插件直接發送 resolve4 DNS 查詢，處理 OneComme 內 queryA ECONNREFUSED 的相容性問題。
- 仍逐一驗證公開 IPv4 並固定連線位址，保留 HTTPS、主動同意、無 Cookie／紀錄／金鑰、逾時與容量限制；不更動統計或資料格式。
- 獨立程序測試不等於 OneComme 實機通過，發布前仍需實際主程式確認。

# v0.6.8

- 頻道設定支援使用者勾選同意後主動向 YouTube 查詢 @名稱／公開頻道首頁，核對名称與 ID 後才保存。UC ID 仍可離線設定。
- 查詢不帶登入 Cookie、紀錄或 OBS 金鑰；只接受 YouTube HTTPS，固定公開 IPv4 連線，不跟隨重新導向，限制 10 秒／4 MiB／5 秒頻率及一筆並行。
- 錯誤與成功訊息顯示在設定欄位旁；查詢中改動網址會丟棄過期結果。隱私與安裝說明同步更新。

# v0.6.7

- 修正 Windows Git checkout 的 CRLF 換行造成版本檢查誤判；仍會阻擋真正過期的版本標籤。
- 新增隔離回歸測試，同時驗證 CRLF 可通過、錯誤版本必須失敗。未更動統計、資料格式或外觀功能。

# v0.6.6

- Windows 測試清理加入有限重試；背景重複停止會等待同一個關閉程序。
- 批次收錄建立識別索引，只複製被修改的紀錄；維持重複事件、衝突隔離與寶石連擊規則。重用日期格式器以降低排行計算成本。
- 管理頁支援最多 512 個變動列的增量同步；版本過期或舊客戶端退回完整同步。只修改外觀時不再重算 TXT 或複製全部歷史。
- 預覽字型使用 LRU 快取，最多同時載入 3 個，一般保留 16 個／64 MiB；當前選用字型最多保護 8 個名稱。淘汰時移除 FontFace，不再以整份大字型作快取鍵。
- 外觀程式拆為靜態介面、畫布、emoji、字型預覽與主協調器，保留九榜／三分頁操作。
- VERSION 為唯一版本來源；發布前檢查白名單、版本、語法與三輪測試，附驗證摘要。新增唯讀權限 Windows GitHub Actions 及共用備份安裝腳本，不自動公開儲存庫或發布 Release。

# v0.6.5

- 前三名排名圖示改為 emoji／文字符號，移除排名圖片上傳。每名可自行輸入，或使用預設 🥇🥈🥉；支援圖示＋數字、只顯示圖示與大小調整。留白保留數字，第四名起照常顯示數字。
- 管理頁即時預覽、OBS 與匯出 CSS 同步使用安全跳脫的文字圖示。支援膚色與連接 emoji，不新增圖片下載或對外請求；背景圖片功能保留。
- 舊排名圖片先備份，再轉換成對應名次獎牌；九榜設定、記錄與既有 OBS 網址不變。首次啟動轉換後請重新整理管理頁與 OBS 來源。

# v0.6.4

- OBS 外觀與輸出改為九張榜單卡片：SC、贈送會員、寶石，各含本場／本月／累計。每張榜獨立編輯，固定顯示正在修改的榜。
- 外觀編輯器分為內容、文字與名次、背景與版面三頁；儲存列固定可見，複製 OBS 網址始終對應這張榜。
- 支援一次性複製已保存外觀到指定其他榜；明確確認覆寫目標，保留目標榜名與顯示名次，不建立持續連動。
- 首次啟動先備份再將舊共用／分期間設定轉為各榜有效外觀；保留紀錄及既有 OBS 網址。未另存名次前沿用舊網址的數量，設定後網址會跟隨該榜的顯示名次。
- 字型資料維持本機去重，失敗預覽仍可選取；無新增對外請求。UTF-8 文字及 CSV 保留原統計與匯出設定。

# v0.6.3

- 字型預覽失敗仍可選取與保存名稱，明確標示替代字型風險，不再阻擋選擇。
- 本機 TTF／OTF 單檔支援 32 MB；TTC 依字族選取常規字面並建立瀏覽器相容資料，不修改系統原檔。詳細別名收起，仍可搜尋。
- SC／贈送會員／寶石可各自設定本場、本月、累計獨立外觀；舊設定維持共用，取消獨立設定與保存共用外觀原子完成。
- 使用中的字型集中去重，最多 64 MB；磁碟外觀只引用獨立字型物件，完整備份仍包含字型。更新保留原資料與前一代復原物件。

# v0.6.2

- Windows 新直播可用獨立確認小視窗加入鎖定頻道，不需開啟管理頁；支援加入、略過與稍後。
- 等待確認時仍保存支持紀錄；確認前不加入未核實頻道的排行榜。回覆時重新驗證連線、頻道與場次，避免舊視窗確認錯誤目標。
- 可在頻道與直播關閉提示；略過只記住該頻道與場次，仍可手動更正，不刪除紀錄。不新增對外查詢。

# v0.6.1

- 本機字型名稱無法載入時，使用經憑證保護的本機字型檔相容讀取；選用的字型資料可隨榜單保存，管理頁與 OBS 使用相同字型資源。
- 字型清單只在可見範圍載入樣張；讀取限 Windows 本機字型資料夾內、清單已列出的 TTF／OTF，單檔 4 MB。
- 本場未確認或尚無支持紀錄時保留預設／自訂榜名，不顯示上一場或假資料；仍尊重標題隱藏設定。
- 使用固定 /channel/UC… 頻道網址連線時，依 OneComme 發布的對應直播網址自動確認場次；標示自動確認來源，不覆蓋其他頻道歸屬。
- 影片網址與無法核對的 @handle 仍保留人工確認；不以觀眾 ID 或連線名稱推斷實況主，不新增對外查詢。

# v0.6.0

- 從本機 OneComme comments.db 或使用者選取的 JSON／JSONL／log 日誌預覽支持事件，依日期與直播 ID 選擇要匯入的場次。
- 來源有提供才顯示直播標題與開台時間；最早收錄時間另行標示，不冒充開台時間。
- 匯入須核對鎖定頻道、逐場勾選與確認，先備份再原子保存；重複事件、內容衝突、受贈、測試及清空規則沿用既有統計政策。
- SQLite 固定本機資料庫唯讀開啟，支援 Node SQLite 與 Windows 內建 SQLite 相容讀取，不下載或上傳資料。
- 字型改用 Windows 完整字族、字面及登錄名稱對應，修正 GDI 名稱截斷與 Bold／Italic 字面未辨識的載入問題。

# v0.5.3

- 使用字型自身的多語名稱建立 local 字型載入規則，修正名稱存在但實際回退到預設字型；預覽與 OBS 共用已保存的規則。
- 字型樣張顯示載入狀態，無法載入時提示匯入字型；不再以備援字型冒充成功樣張。
- 外觀設定分成六個用途群組，一次展開一組，保留既有控制項與保存流程。

# v0.5.2

- 精簡各頁常駐說明；統計範圍與操作細節改為按需展開，保留儲存、頻道確認、OBS 期間與裁切警告。
- 本機字型清單加入在地名称與別名搜尋、字型樣張與即時預覽；各欄位仍可獨立設定。
- SC、贈送會員、寶石各自支援前三名自訂圖示、圖示＋數字／只顯示圖示及大小設定；缺圖回退數字。TXT／CSV 不變。
- 原始碼、安裝包及正式插件統一版本，發行包包含所需全部資源。

# v0.5.1

- 主索引遺失但仍有資料時停止初始化，保留可復原物件。
- 寫入失敗明確顯示警告；事件採有限記憶體佇列每 5 秒重試，溢出明確標示缺漏。
- 大月份改為逐筆合併；年份與儲存檔名格式一致。
- 排名按期間／種類只算一次，快照依資料版本、台北日期與場次快取；管理頁未變更時不重傳完整紀錄。

# v0.5.0

- 按台北月份分檔保存事件，外觀獨立；保留前一代索引，首次升級前備份。
- 空間用量分類、壓縮快照與使用者確認後的備份整理；不刪原始歷史。
- TXT 內容未變時不重寫。
- 建立白名單乾淨原始碼及 Windows 安裝包、SHA-256、安裝說明與 MIT 授權。

# v0.4.1

- 修正 OneComme 擴充功能靜態資源不接受查詢參數的問題；OBS 參數改用 URL fragment。

# v0.4.0

- 正式 OneComme 背景收錄；支援初始快取及 comments envelope、services、meta、meta.clear。
- 紀錄／設定／外觀／重設標記移至私有本機資料夾；一次備份移入舊瀏覽器資料，保留原資料與 OBS 金鑰。
- 背景直接提供三種榜單，不依賴管理頁發布；防止舊頁覆寫背景輸出。
- 移除全部線上 YouTube 解析入口；本機来源限定實際本機 IP，管理與字型 API 加入工作階段驗證。
- 所有可收合區塊使用完整醒目的展開／收合提示，保留原生鍵盤操作。

# 變更紀錄

## 0.3.3 自訂榜單名稱 - 2026-09-10

- OBS 三榜各自保存本場、本月、歷史名稱；留白恢復預設，遵守標題顯示開關。
- 預覽與同步以純文字呈現名稱，不執行 HTML；統計與原始 TXT 不變。

## 0.3.2 背景與畫布 - 2026-09-10

- 背景加入鋪滿／完整／自訂大小、鎖定比例與水平垂直位置。
- 可固定畫布尺寸；編輯預覽按 OBS 基準縮放，提醒來源尺寸及內容超出。
- 十列示意只在工作室顯示，不入紀錄、不發布、不保存示意開關。
- 舊設定預設保持自動高度，三榜各自保存背景與畫布。

## 0.3.1 字型、色彩與間距整合 - 2026-09-10

- 透過本機同步器讀取 Windows 已安裝字型名稱，五個字型欄位共用可搜尋清單。
- 自訂名次色優先於金銀銅，調整色票直接啟用自訂色。
- 三榜外觀增加緊湊／靠右排列、兩段間距、列留白、名稱長度；預設緊湊。
- 移動而非複製舊空格控制，收合為 TXT／原始輸出專用；未變更統計或紀錄儲存。

## 0.3.0 創作者工作室 - 2026-09-10

- 依 Mock 02 整合七頁正式功能，重新安排側欄、文字層級與響應式版面。
- 三榜獨立外觀、四類文字字型與字色、前三名配色、自訂背景與匯入字型。
- IndexedDB 保存外觀，OBS 本機同步資源；完整 JSON 備份包含 appearance。
- 保留頻道驗證、紀錄隔離、去重、場次／月份規則與純文字输出。
- 詳見 docs/CREATOR_STUDIO.md；未打包發行版本、未清空使用者資料。

## 0.1.0 簡表與 OBS CSS 結構 - 2026-09-10

- 縮短狀態列與內容容器，OBS 網址預設收合，複製按鈕維持可用。
- 事件改為六欄簡表與詳情面板，完整留言、識別碼及隔離原因仍保留。
- OBS 新增標題、名次、姓名、數值、各名次與寶石 CSS class；保留原文字輸出並驗證結構一致。
- 詳見 docs/OBS_CSS.md 的樣式範例。未改動事件統計或頻道篩選規則。

## 0.1.0 OBS 同步與分頁改版 - 2026-09-10

- 新版 OBS 只顯示管理頁發布的排行，修復後會員與 SC 使用共用格式；本場、本月、3/5/10 名及寶石同時可用。
- 本機解析器新增受來源限制的同步端點；讀取 token 與發布金鑰分開，逾時顯示中斷而不冒充即時。
- 分離頻道、匯出、OBS、外觀頁，固定桌面導覽、縮短欄位，事件搜尋篩選與每頁 25 筆。
- 保留尚未儲存的表單輸入，避免 OneSDK 更新畫面時覆寫正在調整的設定。
- Figma 僅建立設計檔，後續因 Starter 工具額度停止；HTML 版面直接實作，未套用 Figma 元件庫。

## 0.1.0 會員解析修正 - 2026-09-10

- 支援 OneComme「贈送了 N 個『頻道名』會籍」與 HTML 包裝訊息；只從明確贈送句型擷取。
- 模板事件頁新增手動重新解析工具：先保存完整事件備份，保留修復前欄位及重複副本；只有原留言與身份時間一致、無矛盾已驗證數量的事件可修復。
- 會員去重不再將不相關的金額欄位差異視為會員數量衝突。受贈、測試、清空與真正衝突仍不計榜。

## 0.1.0 操作介面更新 - 2026-09-10

- 放大文字與操作目標、OBS 輸出優先，外觀／匯率／測試與備份收合。
- 頻道設定改為「選實況主、加入直播、下載紀錄」三步；狀態列顯示已加入場數。
- CSV 預設按月，選範圍／內容後使用單一下載按鈕，只顯示當前需要的欄位。
- 管理頁空榜顯示目前無直播、其他月份、未知歸屬、測試或清空等原因，不改變原始計榜規則與 OBS 顯示內容。

## 0.1.0 寶石更新 - 2026-09-10

- 寶石獨立累計、OBS 本場／本月純數字 URL、Node 各期間 TXT、CSV 寶石欄位。
- 依本機 OneComme 9.1.1 資料契約使用 jewels × giftCount；連送僅更新最高份數，未知數值保留待確認，測試／頻道／清空篩選不變。
- 不混入 SC 台幣或贈送會員，不新增寶石排行榜。真實直播寶石端到端測試尚待使用者驗證。

## 0.1.0 功能更新 - 2026-09-10

- 支援公開頻道網址、@handle、直播網址解析名稱及 Channel ID，核對後才儲存。
- 直播歸屬與已鎖頻道不符時拒絕加入；未知或查詢失敗不猜測歸屬。
- 模板模式新增僅監聽 127.0.0.1:11181 的本機解析器及手動啟動檔；不需要 YouTube 登入，不使用第三方查詢站。
- 新增解析契約、網路限制、介面競態與頻道套用測試；完整測試共 61 項通過。

## 0.1.0 - 2026-09-09

- 單一實況主頻道鎖：固定 Channel ID、人工逐場確認、不猜測未知歸屬；套用到各期間榜、OBS、CSV 與 Node TXT。
- OBS URL 包含頻道與已確認場次快照；舊 URL 未設定頻道時不放行。原始紀錄保留，來源未確認與非目標頻道顯示隔離原因。
- 本場不再從頻道首頁連線收到的歷史留言推定影片；使用實際直播網址識別，以防重播或重用連線 ID 誤帶場次。

- 新增管理與匯出頁：指定直播／月份事件明細、SC／贈送會員完整排行、各場彙總 CSV，以及完整 JSON 備份。
- 共用紀錄驗證與排行計算；缺少身份、事件 ID、場次、可靠時間、金額或贈送數量時隔離，不補造本場與時間。
- 新收錄測試模式、複合事件鍵去重、相同鍵內容衝突隔離；保留隔離原因與 CSV 防公式注入。
- 取消模板模式只保存最後 5,000 筆的限制，儲存容量不足時顯示備份警告。
- 明確記錄限制：各瀏覽器／OBS 尚未共用資料庫，舊版資料無法全面事後驗證，JSON 尚無自動還原介面。

- 初始版本：事件正規化、去重、本機持久化、排行榜、OBS TXT、繁中管理頁與人工匯率。
